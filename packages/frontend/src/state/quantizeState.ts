import { frontQuantizeStateType } from "../../../../types";

export const quantizeState: frontQuantizeStateType = {
  flag: false,
  bar: 0,
  beat: 1,
  interval: null,
  currentTime: 0,
  timeout: 0,
  stream: [],
};
