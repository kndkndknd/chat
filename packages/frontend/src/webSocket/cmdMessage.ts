export const cmdMessage = (message) => {
  console.log(message.cmd);
  if (message.cmd === "CLICK") {
  } else if (message.cmd === "BASS") {
  } else if (message.cmd === "WHITENOISE") {
  } else if (message.cmd === "FEEDBACK") {
  }
};
