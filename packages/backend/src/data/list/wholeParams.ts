export const wholeParams: {
  frequency: {
    min: number;
    max: number;
  };
  length: {
    min: number;
    max: number;
  };
  targetArr: string[];
} = {
  frequency: {
    min: 20,
    max: 20000,
  },
  length: {
    min: 200,
    max: 10000,
  },
  targetArr: []
};
