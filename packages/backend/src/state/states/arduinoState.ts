import { arduinoStateType } from "../../../../../types";

export const arduinoState: arduinoStateType = {
  // host: "pi5.local",
  host: "localhost",
  port: 5050,
  connected: false,
  relay: "off",
};
